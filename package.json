{
  "private": true,
  "scripts": {
    "build": "yarn run pulp -- build -t dist/index.js",
    "deps": "yarn && yarn bower i",
    "open": "open dist/index.html",
    "watch": "yarn run pulp -- -w build -t dist/index.js"
  },
  "devDependencies": {
    "bower": "^1.8.0",
    "pulp": "^11.0.2",
    "purescript": "^0.11.6",
    "purescript-psa": "^0.5.1"
  }
}
